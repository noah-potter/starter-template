/**
 * Write a function to output to the sum of a given dimension in a grid.
 * Use the previous sumRow and sumCol functions.
 * Example
  const grid = [
    [1, 2, 3, 4],
    [5, 6, 7, 8],
    [9, 4, 2, 4],
    [1, 9, 3, 6],
  ]
  
  sumDimension(grid, 'row', 1) // 26 (which is the sum of 5, 6, 7, 8)
  sumDimension(grid, 'column', 3) // 26 (which is the sum of 5, 6, 7, 8)
 */
