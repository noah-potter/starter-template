/**
 * Create a function to print the sum of a numbers digits
 * Example
 *   sumOfDigits(123) // 6 (because the digits are 1, 2, 3 and 1+2+3=6)
 *   sumOfDigits(58648) // 31 (because the digits are 5, 8, 6, 4, 8 and 5+8+6+4+8=31)
 *
 * Resources
 *   https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Number/toString
 */
